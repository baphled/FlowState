// Package all provides an aggregator that imports all builtin plugin packages.
// Importing this package causes all builtin plugins to self-register with
// the plugin factory registry via their init() functions.
package all
