Discover available plans in the current project and list them.
Use the bash tool to run: ls ~/.flowstate/plans/ 2>/dev/null || ls ./.flowstate/plans/ 2>/dev/null || echo "no plans dir"
Then use the file tool to read any plan file you find.
Report back with: number of plans found, their IDs, and the first line of each.
Do not execute any plan — this is a discovery dry run.
Proceed immediately with the tool calls; do not ask for confirmation.
