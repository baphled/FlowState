Run this exact bash command and report its output verbatim:

  ls -1 ~/.local/share/flowstate/plans/ 2>/dev/null || echo HARNESS_NO_PLANS_DIR

Issue the bash tool call immediately as your first response. Do not ask for confirmation, do not announce skill loading, do not run a preflight step first. A single bash tool call is the entire task.
